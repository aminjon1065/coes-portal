/**
 * Столбцы, которым разрешено хранить абсолютный адрес (принцип П-5,
 * docs/03-АРХИТЕКТУРА.md § 5.14).
 *
 * Перечень задаёт ИСКЛЮЧЕНИЯ, а не охват: новый столбец попадает под
 * проверку `pnpm check:urls` автоматически, и чтобы его оттуда изъять,
 * нужно осознанно дописать строку сюда.
 *
 * Столбцы перечислены в виде «схема.таблица.столбец».
 */
export const URL_ALLOWLIST: readonly string[] = [
  // Настройки: здесь абсолютный адрес и живёт — BASE_URL, MAP_STYLE_URL.
  'sys.setting.value',
  // Оформление и свойства слоёв приходят из внешних файлов вместе с данными.
  'geo.map_layer.style',
  'geo.map_feature.properties',
];
