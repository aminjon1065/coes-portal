packages:
  - packages/*
  - apps/*

# esbuild собирает свой двоичный файл при установке; без этого не работают
# tsx и vitest. Разрешение явное — pnpm по умолчанию не выполняет скрипты
# установки зависимостей.
allowBuilds:
  esbuild: true
