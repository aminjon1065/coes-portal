/**
 * Схемы этого модуля — из packages/contracts (docs/03-АРХИТЕКТУРА.md § 2).
 * Своих схем модуль не пишет: описание одно (принцип П-2).
 */
export { catalog, catalogListResponse, catalogItemListResponse, catalogItemFilter } from '@coes/contracts';
